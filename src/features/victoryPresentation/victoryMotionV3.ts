/**
 * Victory System V3 — shared presentation motion language (PE / M1U / Rank).
 * Presentation-only; no conductor or server coupling.
 */

/** Premium easing — use across victory surfaces for cohesive feel */
export const V3_EASE = [0.22, 1, 0.36, 1] as const;

export const V3_ENTER_S = 0.48;
export const V3_EXIT_S = 0.52;

/** Cyan vignette inset — PE overlay */
export const V3_CYAN_INSET = 'inset 0 0 100px rgba(0,231,255,0.1)';
