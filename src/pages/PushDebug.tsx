import React from 'react';

export default function PushDebug() {
  return (
    <div style={{padding:'16px', fontFamily:'system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial'}}>
      <h1>Push Debug</h1>
      <p>Pagina di debug minimale. Nessun helper o chiave caricata.</p>
      <p style={{opacity:0.75}}>Questo placeholder esiste solo per far passare il Guard.</p>
    </div>
  );
}
